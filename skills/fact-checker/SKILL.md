---
name: fact-checker
description: Верификация фактов в статье — T1/T2/T3, правит Google Doc, обновляет knowledge-base. Запускать после /editor (и повторного /methodology-gate re-check), перед /quality-gate. Вызывать командой /fact-checker.
---

# Fact-Checker — верификация фактов в статье

Ты верификатор. Проверяешь цифры, факты, утверждения. Не редактируешь язык — только факты.
Никакого T3 с конкретными цифрами. Лучше «по оценкам рынка», чем выдуманный процент.

**Дефолты Фактора** (если нет project.md):
- Project dir: `factor/`
- knowledge-base: `factor/data/knowledge-base-sales-industry.md`
- Articles: `factor/articles/<slug>/pipeline.md`

---

## Шаг 0 — Определить режим входа

```python
import os

# Аргументы: slug=<slug> ИЛИ doc_id=<id> ИЛИ slug + project=<project>
# Приоритет: если передан doc_id — читать напрямую
# Если передан slug — читать pipeline.md → получить google_doc_id

project_dir = "<аргумент project= или 'factor'>"
slug = "<аргумент slug= если передан, иначе None>"
doc_id = "<аргумент doc_id= если передан, иначе None>"

if not doc_id and slug:
    pipeline_path = os.path.join(project_dir, "articles", slug, "pipeline.md")
    # Читать pipeline.md → найти google_doc_id
    # Паттерн: "google_doc_id:  <id>"
```

Если нет ни slug, ни doc_id — спросить: «Передай slug статьи или google_doc_id».

---

## Шаг 1 — Прочитать статью

```javascript
// Через Google Docs MCP
const doc = await mcp__google_docs__get_doc({ documentId: doc_id });
const text = doc.content; // полный текст
```

Если MCP недоступен — спросить: «Вставь текст статьи напрямую или передай WP post_id».

Вывести пользователю:
```
📄 Статья: «{заголовок}»
🔍 Начинаю поиск верифицируемых утверждений...
```

---

## Шаг 2 — Извлечь верифицируемые claims

Прочитать текст и найти ВСЕ:

1. **Числа с контекстом:**
   - `X% [делает что-то]` — «57% клиентов принимают решение до звонка»
   - `X из Y [ситуация]` — «8 из 10 отделов продаж не имеют скриптов»
   - Диапазоны `X–Y%` с конкретным утверждением

2. **Ссылки на источники:**
   - «По данным [источник]...»
   - «Исследование [название/компания] показало...»
   - «[Компания] выяснила / опросила / обнаружила...»

3. **Конкретные события и даты:**
   - «В [год] компания [X] сделала [Y]»
   - Исторические факты с датами

4. **Утверждения-бенчмарки:**
   - «Норма конверсии из лида в КЭВ — X%»
   - «Средний чек по рынку — X руб.»

Составить список с цитатой и предполагаемым источником:

```
[1] «57% пути клиент проходит сам до первого контакта с продавцом»
    Вероятный источник: CEB/Gartner

[2] «Конверсия из КЭВ в сделку 20–40%»
    Вероятный источник: внутренние данные ФП или отраслевой бенчмарк

[3] «8 из 10 руководителей не слушают звонки менеджеров»
    Вероятный источник: не указан
```

---

## Шаг 3 — Верифицировать каждый claim

Для каждого claim:

```
1. WebSearch: «[цитата буквально]» ИЛИ «[источник] [исследование] [год]»
2. WebSearch на английском если claim похож на западные данные
3. WebFetch первой релевантной ссылки — найти оригинал
```

---

## Шаг 3.5 — Механическая верификация URL (обязательно для T1/T2)

**Принцип из deep-research:** один верифицированный источник весомее трёх LLM, которые просто согласились. Consensus без доказательства = повышенный риск галлюцинации.

Для каждого кандидата в T1/T2 — два механических теста:

**Тест 1 — URL живой:**
```bash
curl -s -o /dev/null -A "Mozilla/5.0" --max-time 10 -w "%{http_code} %{redirect_url}\n" "<URL>"
# 200 + пустой redirect_url → живой ✅
# 301/302 → повторить команду для redirect_url (пошагово, чтобы видеть смену домена)
#   редирект уводит на ДРУГОЙ домен → T3 автоматически
# 404, 403 → T3 автоматически
```
Не использовать `-L` + `head -1`: так виден только первый статус цепочки, а критерий «301 на другой домен» проверить нельзя.

**Тест 2 — Цитата существует в тексте:**
```javascript
// WebFetch страницы → искать ключевую фразу (5–8 слов) из цитаты
// Найдена дословно или близко → T1/T2 подтверждён ✅
// Не найдена → T3 (источник существует, но цитата не там)
```

**Флаг ⚠️ CONSENSUS-ONLY** — выставлять если:
- WebSearch дал 3+ результата с одинаковой цифрой
- Но ни один не ведёт на первичный источник (все ссылаются друг на друга)
- Это признак цепочки перепечаток — цифра могла возникнуть ниоткуда

```
⚠️ CONSENSUS-ONLY: «8 из 10 руководителей не слушают звонки»
   Найдено в 5 статьях — все без ссылки на исследование.
   Решение: T3, убрать цифру.
```

**Итоговая матрица решений:**

| URL статус | Цитата найдена | Итог |
|-----------|---------------|------|
| 200 | Да | T1/T2 ✅ |
| 200 | Нет | T3 (источник есть, цитата нет) |
| не 200 | — | T3 (URL мёртв) |
| Нет URL | 3+ согласны | ⚠️ CONSENSUS-ONLY → T3 |
| Нет URL | 1–2 согласны | T3 |

---

**Принцип T1/T2/T3/X/FAIL:**

| Уровень | Условие | Что делать в статье |
|---------|---------|---------------------|
| T1 | URL 200 + цитата найдена в тексте оригинала | Оставить с атрибуцией |
| T2 | Крупное СМИ (Forbes, РБК, Коммерсант), URL 200, ссылается на конкретный отчёт | Оставить с атрибуцией |
| T3 | URL мёртв / цитата не найдена / нет первичника | Убрать число, написать «по оценкам рынка» |
| X | Источник, который не читал (не открывал оригинал) | **Никогда не называть** источник в статье; факт → T3 без атрибуции |
| FAIL | Claim противоречит найденным источникам | Удалить claim полностью |

**Правило T3:** Если не нашёл источник — это T3. Нельзя оставить «57%» без T1/T2.
Фраза «по оценкам рынка» — не трусость, это честность.

**Внутренние данные ФП** (конверсии из нашей практики):
- Не нужно верифицировать через WebSearch
- Пометить как `[✓ ФП-данные]`
- Убедиться что это реально внутренние данные, не "мы обычно видим что..."

---

## Шаг 3.7 — Проверка кейсов клиента

Каждый кейс клиента в статье («в проекте с компанией X…», «клиент из ниши Y…», цифры «было/стало») сверить со списком реальных кейсов — `cases_url` из `<project>/project.md` (для factor: yoursite.ru/case/).

- WebFetch списка кейсов → найти соответствие по нише и цифрам
- Кейс найден → пометить `[✓ кейс]`
- Кейс НЕ найден → флаг `⚠️ [кейс не верифицирован]` комментарием в Google Doc; **не публиковать без подтверждения владельца проекта** — сообщить в финальном выводе

---

## Шаг 4 — Внести правки в Google Doc

Для T3 (убрать цифру):
```javascript
await mcp__google_docs__replace_text({
  documentId: doc_id,
  oldText: "57% клиентов принимают решение до звонка",
  newText: "по оценкам рынка, большинство клиентов частично принимают решение до первого контакта с продавцом"
});
```

Для FAIL (удалить claim):
```javascript
await mcp__google_docs__replace_text({
  documentId: doc_id,
  oldText: "[полная фраза с неверным утверждением]",
  newText: ""  // или переформулировать без конкретной цифры
});
```

**Fallback при ошибке 500:** известный баг Google Docs API — replaceAllText иногда возвращает 500. Обход: получить структуру документа (get-doc-structure), найти индексы фразы, затем удалить диапазон через `deleteContentRange` и вставить новый текст через `insertText` по тем же индексам. Не ретраить replace-text бесконечно.

**🔒 Комментарии клиента НЕ резолвить.** Комментарии, оставленные клиентом/человеком в Google Doc, оставлять открытыми — они нужны для диффа. Резолвить можно только свои служебные комментарии.

Добавить итоговый комментарий:
```javascript
await mcp__google_docs__add_comment({
  documentId: doc_id,
  comment: "✅ Fact-check завершён.\n" +
    "T1: A утверждений подтверждено\n" +
    "T3: B утверждений исправлено (убраны цифры)\n" +
    "FAIL: C утверждений удалено (противоречат источникам)\n" +
    "Данные клиента: N оставлено без изменений"
});
```

---

## Шаг 5 — Обновить knowledge-base.md

Все T1/T2 факты добавить в базу знаний:

```markdown
# Добавить в factor/data/knowledge-base-sales-industry.md

## [Дата добавления] — статья «[H1]»

| Факт | Источник | Год | Уровень |
|------|----------|-----|---------|
| 57% пути клиент проходит до контакта с продавцом | CEB/Gartner, "The Challenger Customer" | 2015 | T1 |
```

Проверить: нет ли уже этого факта в knowledge-base.md (дубликаты не нужны).

---

## Шаг 6 — Обновить pipeline.md

```markdown
# В articles/<slug>/pipeline.md найти строку:
# | 6. Fact-check | /fact-checker | ⏳ | — | — |
# Заменить на:
| 6. Fact-check | /fact-checker | ✅ | {date} | T1:A T3:B FAIL:C |

# В секции ## Fact-check добавить:
<!-- [✓ T1] "57% пути клиент проходит сам" → CEB/Gartner, The Challenger Customer, 2015 -->
<!-- [→ T3] "8 из 10 не слушают звонки" → цифра убрана, заменено на "по оценкам рынка" -->
<!-- [✗ FAIL] "цитата" → удалено, противоречит источникам -->
```

---

## Финальный вывод пользователю

```
✅ Fact-check завершён: «{заголовок}»

Проверено утверждений: N
  [✓ T1] A — подтверждено с источником
  [✓ T2] B — подтверждено через СМИ
  [→ T3] C — цифры убраны, заменено на «по оценкам рынка»
  [✗ FAIL] D — удалено (противоречит источникам)
  [✓ ФП] E — внутренние данные клиента, оставлены
  [⚠️ кейс] F — кейсы не найдены на cases_url, ждут подтверждения владельца

Добавлено в knowledge-base.md: {A+B} фактов

Google Doc: https://docs.google.com/document/d/{doc_id}/edit
Следующий шаг: /quality-gate slug={slug}
```

---

## Аргументы

```
/fact-checker slug=rabota-s-vozrazheniyami-v-prodazhakh
/fact-checker doc_id=1bgiyrn4ZBcbzN2fxfxgW5GO3w2KehDzy8oC2_Z1EnkE
/fact-checker slug=rabota-s-vozrazheniyami project=crmgroup
```

- `slug` — папка `articles/<slug>/pipeline.md`
- `doc_id` — напрямую, без pipeline.md
- `project` — дефолт `factor`

---

## Gotchas

- **T3 ≠ плохо** — «по оценкам рынка» честнее, чем цифра без источника. Не пытаться "найти хоть что-то" лишь бы повысить уровень до T1.
- **Gartner/CEB "57%"** — очень часто цитируется неточно. Оригинал: "The Challenger Customer" (2015), не "Challenger Sale". Проверять всегда.
- **Бенчмарки клиента** — сверять с `<project>/data/company-context.md`, не с памятью. Для factor: доведение до КЭВ-встречи 70–90%, конверсия из КЭВ-встречи 20–40% (универсальной нормы нет — зависит от типа КЭВ). Внутренние данные не нужно верифицировать через WebSearch, но убедиться, что в тексте написано «из нашей практики», а не «средний показатель по рынку».
- **Запрещённые к цитированию источники** — список `do_not_mention` из `<project>/project.md` (для factor: Meduza, Нескучный бизнес и др.) — не цитировать даже как T2. Такой источник в тексте → убрать атрибуцию и понизить факт до T3 или заменить источник.
- **Длинные цитаты** — для replace-text брать уникальную фразу из 5–8 слов, не весь абзац. Длинные замены рискуют задеть форматирование.
- **Не редактировать язык** — только факты. Если видишь ИИ-обороты рядом с фактом, пропустить — это задача /editor.
- **Несколько утверждений в одном предложении** — обрабатывать по одному. Не заменять целый абзац за раз.
- **knowledge-base.md может не существовать** — создать если нет (путь из project.md или дефолт; для factor: `factor/data/knowledge-base-sales-industry.md` с заголовком `# База верифицированных фактов — Фактор Продаж`).
- **pipeline.md может не существовать** — если статья создана без /researcher, создать из шаблона `<project>/articles/_template/pipeline.md`. Не блокировать работу.
- **curl на русскоязычные источники** — User-Agent обязателен: многие РБК/Forbes.ru без UA возвращают 403. Редиректы проходить пошагово (команда из Теста 1 показывает `redirect_url`), не через `-L` — иначе не увидеть уход на другой домен.
- **CONSENSUS-ONLY — главная ловушка** — если одна цифра (например "70% сделок проигрывается на этапе квалификации") встречается в 10 SEO-статьях без ссылки на исследование, это цепочка перепечаток, не факт. Всегда искать первоисточник через WebSearch `site:hh.ru OR site:rabota.ru OR site:rbc.ru [цифра]` для русских данных.
- **WebFetch ищет цитату нечётко** — если дословное совпадение не найдено, попробовать 3–4 ключевых слова из цитаты. Авторы часто перефразируют при цитировании. Если смысл совпадает → T1, если данные другие → FAIL (удалить claim).
- **Не резолвить комментарии клиента** в Google Doc — они нужны для диффа. Резолвить только свои служебные.
